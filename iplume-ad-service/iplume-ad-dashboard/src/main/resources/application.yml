server:
  port: 7002
spring:
  application:
    name: ad-dashboard

# eureka config.
eureka:
  client:
    service-url:
      defaultZone: http://server1:8000/eureka/

# 暴露规则.
management:
  endpoints:
    web:
      exposure:
        include: "*"